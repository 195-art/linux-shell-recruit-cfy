## 问题：为什么 Shell 中 $var 和 "$var" 有时结果不同？

`$var` 不写双引号的时候，shell拿到变量里面的内容，遇到空格就会把内容拆成好几段。
如果文件名里面带有空格，原本的一个文件会被拆成多个部分，程序就识别不到正确的文件，运行就会出错。

`"$var"` 加上双引号之后，就不会按空格拆分内容，会把里面所有内容当成一整个整体，就算里面有空格也可以正常处理带空格的文件名。

原来脚本里的 `$@`、`$destination`、`$file` 都没有加双引号，所以处理带空格的文件就会出现bug，给这些变量加上双引号之后问题就解决了。
